import { createContext } from "react";

export const LoginContext = createContext({
  //   username: "",
  //   setChartData: () => {},
}); //is this javascript object passed over throughout? sent in through app.js?
