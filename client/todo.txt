1. Build the chart
- get data formatted into chart 
- connect API
- format ui element

2. Search Bar
- Research the bootstrap form (figure out best way to make the search bar)
- wire the input to a state hook.  useState() - this can be referenced across many files as a "context"
setContext -> store -> access context wherever 

WatchList
